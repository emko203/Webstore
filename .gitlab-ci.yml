image: maven:latest

cache:
  paths:
    - .m2/repository/
    - target/

build:
  stage: build  
  script:
    - $MAVEN_HOME/bin/mvn clean
    - mvn $MAVEN_CLI_OPTS compile

test:
  stage: test
  script:
    - mvn $MAVEN_CLI_OPTS test
